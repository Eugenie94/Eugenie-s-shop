<form method="post" action="{{ path('panier_delete', {'id': panier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ panier.id) }}">
    <button class="btn btn-outline-danger my-2 my-sm-0">Delete<i class="fa-solid fa-trash" style="padding-left:10px;"></i></button>
</form>