{% extends 'base.html.twig' %}

{% block title %}{{ 'produit.newTitle'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'produit.newH1'|trans }}</h1>

    {{ include('produit/_form.html.twig') }}

    <a href="{{ path('produit_new', {'id': produit.id}) }}">{{ 'produit.newReturnList'|trans }}</a>
{% endblock %}
