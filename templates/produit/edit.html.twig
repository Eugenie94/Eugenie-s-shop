{% extends 'base.html.twig' %}

{% block title %}{{ 'produit.editTitle'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'produit.editH1'|trans }}</h1>

    {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('home') }}">{{ 'produit.editReturnList'|trans }}</a>

    {{ include('produit/_delete_form.html.twig') }}
{% endblock %}
