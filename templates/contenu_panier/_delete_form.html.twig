<form method="post" action="{{ path('contenu_panier_delete', {'id': contenu_panier.id}) }}" onsubmit="return confirm('"{{ 'contenuPanier.alertDeleteProdPanier'|trans }}"');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contenu_panier.id) }}">
   <button class="btn btn-outline-danger my-2 my-sm-0">{{ 'contenuPanier.deleteProdPanier'|trans }}<i class="fa-solid fa-trash" style="padding-left:10px;"></i></button>
</form>

